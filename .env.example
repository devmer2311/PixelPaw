# Copy this file to .env and set the version you want to build/release.
# scripts/set-version.js reads VERSION and writes it into package.json at build time.
# Accepts "1.2.0" or "v1.2.0". Last GitHub release was v1.1.
VERSION=1.2.0
